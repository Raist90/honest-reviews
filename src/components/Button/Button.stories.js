import Button from "./Button";

const meta = {
  title: "Button",
  component: Button,
};

export default meta;

export const Primary = {
  render: () => <Button label="Button">Vai al link</Button>,
};
